import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'md.filosoft.fibonaccigame',
  appName: 'Fibonacci Game',
  webDir: 'dist/fibonacci-game/browser'
};

export default config;
